{
  "name": "pumpkin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "rsync -av --exclude node_modules --exclude .git . pi@192.168.0.14:~/pumpkin",
    "run": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.5.1"
  }
}
